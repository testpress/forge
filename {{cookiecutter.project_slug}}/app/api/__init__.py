# django-ninja API package.
#
# The NinjaAPI instance lives in `app/api/urls.py` and is wired into the
# project's URLconf by `config/urls.py`, so the API is served by the same
# Django process, through the same middleware, as the rest of the site.
